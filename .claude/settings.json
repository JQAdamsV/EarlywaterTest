{
    "$comment": "AI-Builder instance settings for pd.tiknix. Runs under --permission-mode bypassPermissions; the bubblewrap jail is the security boundary. The PreToolUse hook below is the in-instance scope guard (protects billing/vendor/guard files). Do not remove it.",
    "hooks": {
        "PreToolUse": [
            {
                "matcher": "Write|Edit|MultiEdit|NotebookEdit|Bash",
                "hooks": [
                    {
                        "type": "command",
                        "command": "php \"$CLAUDE_PROJECT_DIR/.claude/guard.php\""
                    }
                ]
            }
        ]
    },
    "enableAllProjectMcpServers": true
}
